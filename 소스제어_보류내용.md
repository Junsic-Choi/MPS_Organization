# 🚀 소스제어(Source Control) 보류 내용 요약

VSCode 소스 제어에 커밋하실 때 아래 내용을 복사해서 사용하시면 됩니다.

---

### [커밋 메시지 제목]
Perf: Auto_Extract_Dynamic_V3.ps1 엑셀 데이터 메모리 일괄 읽기 최적화

### [커밋 상세 내용]
1. **엑셀 데이터 메모리 일괄 캐싱 (COM 통신 병목 제거)**
   - 기존: `Cells.Item(r, c).Value2`로 셀마다 COM 통신을 발생시켜 매우 느림
   - 변경: `$ws.Range.Value2`를 이용해 전체 데이터를 파워쉘의 2차원 배열(`$dataMatrix`)로 1초 만에 통째로 복사한 뒤 메모리 위에서 탐색. 체감 지연시간 대폭 감소.

2. **Collection.List를 통한 배열 삽입 최적화**
   - 기존: `$results +=` 방식의 비효율적인 배열 확장 사용
   - 변경: `.NET`의 `[System.Collections.Generic.List]::new()` 및 `.Add()` 메서드를 도입하여 대량 행 추가 시 메모리 재할당 부하 제거.

3. **비어있는 셀(병합셀) 데이터 전파 로직 도입**
   - `currSite`, `currGroup`, `currModel`, `currRPM` 변수를 통해 이전 행(Row)의 값을 기억하고, 빈 셀이 등장해도 자동으로 값을 채워 CSV에 온전히 매핑되도록 처리.
