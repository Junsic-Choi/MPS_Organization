# 🚀 소스제어(Source Control) 보류 내용 요약

VSCode 소스 제어에 커밋하실 때 아래 내용을 복사해서 사용하시면 됩니다.

---

### [커밋 메시지 제목]
Feat: VCF 시리즈 및 RPM별 특수 매핑 로직 고도화 적용

### [커밋 상세 내용]
1. **VCF850 및 VF8 시리즈 전용 매핑 로직 구현**
   - VCF850 기종에 대해 VF8 제품명 체계 적용.
   - RPM 사양에 따른 분기(12K -> -0-, 18K -> -1-) 및 특수 문자열 매핑 엔진 고도화.

2. **범용 RPM 접미사(H/H, SONE) 처리 규칙 도입**
   - RPM 필드 내 `(H/H)` 감지 시 제품명에 `HT` 삽입 로직 전역 적용.
   - RPM 필드 내 `(SONE)` 감지 시 제품명에 `SO` 삽입 로직 전역 적용.

3. **최종 4650행 데이터 무결성 검토 완료**
   - 2월~7월 생산 데이터에 대해 정제된 월 표기 및 고도화된 기종/제품명 매핑을 적용하여 대시보드용 최종 CSV 생성 완료.
