# 🚀 소스제어(Source Control) 보류 내용 요약

VSCode 소스 제어에 커밋하실 때 아래 내용을 복사해서 사용하시면 됩니다.

---

### [커밋 메시지 제목]
Feat: GT 시리즈 정밀 매핑 및 4650행 추출 정합성 강화

### [커밋 상세 내용]
1. **GT 시리즈 전용 매핑 규칙 적용**
   - `GT3100 -> GT31`, `GT2600 -> GT26` 제품 prefix 변환 로직 구현.
   - `XLA`, `LM`, `L` 등 기종별 특수 접미사를 제품명에 그대로 보존하도록 Regex-based 처리.

2. **4650행 데이터 정밀 슬라이싱 및 필터링**
   - 2~7월 데이터 중 정확히 상위 4650행만 추출되도록 엄격한 슬라이싱 로직(slice 0-4651) 확정.
   - GT 시리즈가 누락되지 않도록 필터링 인덱스 재검증 완료.

3. **고도화된 통합 매핑 엔진 확립**
   - VCF, DHF, DBD, HFP 및 GT 시리즈까지 모든 특수 명칭 체계를 처리하는 `process_fuzzy.js` 스크립트 고도화 완료.
